{
  "name": "avatarka-monorepo",
  "private": true,
  "packageManager": "pnpm@9.14.2",
  "scripts": {
    "build": "turbo run build",
    "build:static": "turbo run build && cd apps/demo && pnpm build",
    "dev": "turbo run dev",
    "check": "tsc --noEmit -p packages/avatarka/tsconfig.json && tsc --noEmit -p packages/avatarka-react/tsconfig.json && tsc --noEmit -p apps/demo/tsconfig.json",
    "clean": "turbo run clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@testing-library/react": "^16.1.0",
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^2.1.8",
    "jsdom": "^25.0.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
